name: CI
on: [push, pull_request, workflow_dispatch]
jobs:
  ci:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - run: pnpm install --frozen-lockfile
      - run: npx lefthook run pre-commit --all-files --no-tty
